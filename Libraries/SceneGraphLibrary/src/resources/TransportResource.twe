class TransportResource extends JointedModelResource {
    TransportResource(TextString name) {
        super(name: name);
    }

    @CompletelyHidden static JointId[] EXPECTED_JOINTS <- new JointId[0];
}
