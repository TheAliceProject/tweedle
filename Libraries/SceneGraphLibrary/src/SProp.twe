class SProp extends SJointedModel {
  SProp(PropResource resource) {
    super(resource: resource);
  }

  SProp(TextString resource, JointId root) {
    super(resource: resource, root: root);
  }

  SProp(TextString resource, JointId[] roots) {
    super(resource: resource, roots: roots);
  }
}
