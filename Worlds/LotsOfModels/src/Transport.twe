class Transport extends STransport models Transport {
  Transport(TextString resource) {
    super(resource: resource);
  }

  Transport(TextString resource, JointId root) {
    super(resource: resource, root: root);
  }
}
